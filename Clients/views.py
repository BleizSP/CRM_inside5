from django.shortcuts import render
from .models import Client
from django.http import HttpResponse


# Create your views here.
# def index(request):
#     zapytanie = Client.objects.all()
#     return HttpResponse(zapytanie)